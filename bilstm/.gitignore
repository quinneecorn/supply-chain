# Secrets
.env

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Model artifacts — ignore old epoch snapshots only
checkpoints/bilstm_ep06*.pt
checkpoints/bilstm_ep08*.pt
checkpoints/bilstm_ep10*.pt

# Data (large — share via Drive/Supabase; keep .env.example only)
final_merged_data.csv
data/augmented_train.csv

# OS / IDE
.DS_Store
.idea/
.vscode/
